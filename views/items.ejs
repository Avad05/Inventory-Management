<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Inventory</title>
    <link rel="stylesheet" href="/uploads/styles.css">
</head>
<body>
    <li>
        <img src="<%= details.image_url.trim() %>" alt="<%= details.name %>">

        <div class="controls">
            <h1><%= details.name %></h1>
            <p>Current Quantity: <strong><%= details.quantity %></strong></p>

            <div class="quantity-box">
                <form action="/items/<%= details.id %>" method="POST">
                    <input type="hidden" name="action" value="decrement">
                    <button type="submit" class="qty-btn" <%= details.quantity <= 0 ? 'disabled' : '' %>>−</button>
                </form>

                <form action="/items/<%= details.id %>" method="POST">
                    <input type="hidden" name="action" value="increment">
                    <button type="submit" class="qty-btn">+</button>
                </form>
            </div>

            <form action="/items/<%= details.id %>" method="POST" class="price-form">
                <input type="hidden" name="action" value="changePrice">
                <label>Update Price: <strong>₹</strong></label><br>
                <input type="number" name="newPrice" value="<%= details.price %>" step="1.0" required>
                <button type="submit">Save Price</button>
            </form>

            <form action="/items/<%= details.id %>/delete" method="POST" class="delete-form" onsubmit="return confirm('Delete this product?');">
                <button type="submit" class="delete-btn">
                       Delete Product
                </button>
            </form>
        </div>
    </li>
</body>
</html>
